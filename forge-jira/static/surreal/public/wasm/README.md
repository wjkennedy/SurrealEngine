Place the Emscripten output here:

- `surreal-engine.js`
- `surreal-engine.wasm`
- `surreal-engine.data` when game data is packaged with `--preload-file`

Use `npm run stage:wasm -- ../build-wasm` from `forge-jira/` to copy the files before `npm run build:ui`.
